def kth_smallest_element_in_union_of_multiple_sorted_arrays(arrays, k)
  (0...arrays.length).each do |i|
    array = arrays[i]
    other_arrays = arrays.drop(i + 1)

    while
    if limits[i]

    # look for the thing which is kth in arrays[i]

    # if it's exactly there, done
    # otherwise, go to the next array
  end
end
